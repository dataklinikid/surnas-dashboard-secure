# Stage 8B8G — CLI Fix v2

Patch ini mengganti opsi command yang bentrok dengan opsi global Django
`--version` menjadi `--metadata-version`. Tidak ada perubahan data, migration,
atau perubahan pada algoritme multiple-answer.
